w = float(input('Type the width od the wall: '))
h = float(input('Type the height of the wall: '))
total_area = w * h
res = total_area / 2
print('The total litres that you need to paint this wall is: {}'.format(res))